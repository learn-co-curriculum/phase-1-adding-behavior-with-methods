describe("cat", () => {
  it("has a class of cat", () => {
    expect(Cat).to.exist
  })
})
describe("dog", () => {
  it("has a class of dog", () => {
    expect(Dog).to.exist
  })
})
describe("bird", () => {
  it("has a class of Bird", () => {
    expect(Bird).to.exist
  })
})
